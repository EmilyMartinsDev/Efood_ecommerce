class Produto {
  id: number
  name: string
  banner: string
  description: string

  constructor(id: number, name: string, banner: string, description: string) {
    this.id = id
    this.name = name
    this.banner = banner
    this.description = description
  }
}
export default Produto
